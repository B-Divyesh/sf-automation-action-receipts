# Demo scenario

`action-receipts demo` creates this scenario without changing the current directory:
an operations-approved documentation deployment, a policy check, a recorded build,
and a hashed release-notes artifact. It writes signed JSON and a self-contained HTML
report to a newly created temporary directory.
